package com.project.mission.repository;

import com.project.mission.entity.BoardWrite;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardWriteRepository extends JpaRepository<BoardWrite, Long> {
}
